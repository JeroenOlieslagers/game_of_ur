*.DS_Store

# Solved maps and their working files. These run from hundreds of MB to several
# GB and are either downloaded or regenerated: see scripts/download_models.py.
models/
*.rgu
*.rgu.partial
*.checkpoint
*.checkpoint.*
*.layers/

# Simulation and analysis outputs, also regenerated.
results/
random_results/
bench/
logs/
long_game/long_game_*.out
__pycache__/
*.jld2
*.csv

# Rust build artefacts, and the toolchains the cluster scripts install in-tree.
rust/target/
.cargo/
.rustup/
.toolchain/
.build.lock

# Resolved per machine for this package.
Manifest.toml

# Vendored BayesElo build (see analysis/bayeselo_readme.txt).
BayesElo

# The manuscript is deliberately not in this repository. Once the revision is
# resubmitted, the final PDF and its figures can be added on purpose; the LaTeX
# sources and build artefacts stay out.
paper/
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.synctex.gz
*.toc
*.out

# Stray analysis outputs; these belong outside the repo.
ruleset_analysis/
*_weights.txt
